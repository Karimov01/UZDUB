"use client";
import Link from "next/link";
import { Search, ChevronRight } from "lucide-react";
import { useMemo, useState } from "react";
export type GenreCard={slug:string;name:string;color:string;count:number;emoji:string};
export default function GenreGrid({genres}:{genres:GenreCard[]}){const[q,setQ]=useState("");const items=useMemo(()=>genres.filter(g=>g.name.toLowerCase().includes(q.toLowerCase())),[genres,q]);return <><div className="relative max-w-md mb-7"><Search className="absolute left-3 top-3 h-4 w-4 text-violet-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Janrni qidirish..." className="w-full rounded-xl py-2.5 pl-9 pr-3 bg-white/[.03] text-white outline-none" style={{border:"1px solid var(--border)"}}/></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">{items.map(g=><Link key={g.slug} href={`/janr/${g.slug}`} className="group rounded-2xl p-5 transition-transform hover:-translate-y-1" style={{background:`linear-gradient(135deg,${g.color}20,rgba(11,12,21,.86))`,border:`1px solid ${g.color}70`}}><div className="flex justify-between"><span className="text-3xl">{g.emoji}</span><ChevronRight className="h-5 w-5 group-hover:translate-x-1 transition-transform" style={{color:g.color}}/></div><h2 className="mt-5 text-white font-semibold">{g.name}</h2><p className="text-sm mt-1" style={{color:"var(--text-muted)"}}>{g.count} ta kontent</p></Link>)}</div>{!items.length&&<p className="py-14 text-center" style={{color:"var(--text-muted)"}}>Bu nomdagi janr topilmadi.</p>}</>}
