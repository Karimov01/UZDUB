"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { Check, ChevronLeft, ChevronRight, Crown, Pencil, Search, Shield, Users, UserRound, X } from "lucide-react";
import type { StoredUser, UserStats } from "@/lib/movies-store";

type Data = { users: StoredUser[]; total: number; page: number; size: number; stats: UserStats };
const blank: Data = { users: [], total: 0, page: 1, size: 20, stats: { total: 0, free: 0, premium: 0, admins: 0 } };
const ago = (date: string) => { const minutes = Math.floor((Date.now() - new Date(date).getTime()) / 60000); return minutes < 2 ? "Hozir" : minutes < 60 ? `${minutes} daqiqa oldin` : minutes < 1440 ? `${Math.floor(minutes / 60)} soat oldin` : new Date(date).toLocaleDateString("uz-UZ"); };

export default function UsersPage() {
  const [data, setData] = useState<Data>(blank); const [query, setQuery] = useState(""); const [q, setQ] = useState(""); const [filter, setFilter] = useState("ALL"); const [page, setPage] = useState(1); const [selected, setSelected] = useState<string[]>([]); const [drawer, setDrawer] = useState<StoredUser | null>(null); const [saving, setSaving] = useState(false);
  useEffect(() => { const timer = setTimeout(() => setQ(query), 350); return () => clearTimeout(timer); }, [query]);
  const load = useCallback(async () => { const params = new URLSearchParams({ page: String(page), size: "20", q }); if (filter === "PREMIUM" || filter === "FREE") params.set("subscription", filter); if (filter === "ADMIN" || filter === "USER") params.set("role", filter); if (filter === "ACTIVE" || filter === "BLOCKED") params.set("status", filter); const response = await fetch(`/api/admin/users?${params}`, { cache: "no-store" }); const json = await response.json(); if (response.ok) { setData(json); setSelected([]); } }, [page, q, filter]);
  useEffect(() => { void load(); const timer = setInterval(() => void load(), 20000); return () => clearInterval(timer); }, [load]);
  const pages = Math.max(1, Math.ceil(data.total / data.size)); const selectedUsers = useMemo(() => data.users.filter((user) => selected.includes(user.id)), [data.users, selected]);
  const toggleAll = () => setSelected(selected.length === data.users.length ? [] : data.users.map((user) => user.id));
  const save = async () => { if (!drawer) return; setSaving(true); const response = await fetch(`/api/admin/users/${drawer.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ role: drawer.role, subscriptionType: drawer.subscriptionType, isActive: drawer.isActive }) }); const json = await response.json(); setSaving(false); if (!response.ok) return alert(json.error ?? "Saqlab bo'lmadi"); setDrawer(null); void load(); };
  const stats = [{ title: "Jami foydalanuvchilar", value: data.stats.total, icon: Users, color: "#a855f7" }, { title: "Oddiy foydalanuvchilar", value: data.stats.free, icon: UserRound, color: "#3b82f6" }, { title: "Premium foydalanuvchilar", value: data.stats.premium, icon: Crown, color: "#f59e0b" }, { title: "Admin foydalanuvchilar", value: data.stats.admins, icon: Shield, color: "#d946ef" }];
  return <div className="pb-28"><div className="mb-6"><h1 className="text-2xl font-bold text-white">Foydalanuvchilar</h1><p className="text-sm mt-1" style={{color:"var(--text-muted)"}}>Telegram orqali ro‘yxatdan o‘tgan haqiqiy foydalanuvchilar</p></div><div className="grid grid-cols-2 xl:grid-cols-4 gap-4 mb-6">{stats.map((item) => <div key={item.title} className="p-5 rounded-2xl relative overflow-hidden" style={{background:"linear-gradient(145deg,rgba(19,18,32,.96),rgba(10,11,20,.96))",border:`1px solid ${item.color}70`,boxShadow:`0 0 24px ${item.color}16`}}><item.icon className="h-6 w-6" style={{color:item.color}}/><p className="text-xs mt-4" style={{color:"var(--text-muted)"}}>{item.title}</p><p className="text-3xl text-white font-bold mt-1">{item.value.toLocaleString("uz-UZ")}</p></div>)}</div><div className="rounded-2xl overflow-hidden" style={{background:"rgba(16,16,27,.82)",border:"1px solid var(--border)"}}><div className="p-4 flex flex-col lg:flex-row gap-3" style={{borderBottom:"1px solid var(--border)"}}><div className="relative flex-1"><Search className="h-4 w-4 absolute left-3 top-3 text-violet-400"/><input value={query} onChange={(e)=>{setQuery(e.target.value);setPage(1)}} placeholder="Foydalanuvchini qidirish..." className="w-full py-2.5 pl-9 pr-3 rounded-xl bg-black/20 text-sm text-white outline-none" style={{border:"1px solid var(--border)"}}/></div><select value={filter} onChange={(e)=>{setFilter(e.target.value);setPage(1)}} className="rounded-xl px-3 text-sm text-white bg-black/20" style={{border:"1px solid var(--border)"}}><option value="ALL">Barchasi</option><option value="FREE">Oddiy</option><option value="PREMIUM">Premium</option><option value="USER">User</option><option value="ADMIN">Admin</option><option value="ACTIVE">Faol</option><option value="BLOCKED">Bloklangan</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-[920px]"><thead><tr style={{background:"rgba(255,255,255,.025)"}}><th className="p-4"><input aria-label="Barchasini tanlash" type="checkbox" checked={data.users.length>0&&selected.length===data.users.length} onChange={toggleAll}/></th>{["Foydalanuvchi","Telegram ID","Maqom","Role","A’zo bo‘lgan","Oxirgi faollik","Holat","Amallar"].map((title)=><th key={title} className="p-4 text-left text-xs font-medium" style={{color:"var(--text-muted)"}}>{title}</th>)}</tr></thead><tbody>{data.users.map((user) => <tr key={user.id} className="transition-colors hover:bg-violet-500/[.04]" style={{borderTop:"1px solid var(--border)",background:selected.includes(user.id)?"rgba(124,58,237,.09)":undefined}}><td className="p-4"><input aria-label={`${user.firstName}ni tanlash`} type="checkbox" checked={selected.includes(user.id)} onChange={()=>setSelected((ids)=>ids.includes(user.id)?ids.filter((id)=>id!==user.id):[...ids,user.id])}/></td><td className="p-4"><div className="flex items-center gap-3"><div className="h-9 w-9 rounded-full overflow-hidden flex items-center justify-center text-white font-bold" style={{background:"linear-gradient(135deg,#7c3aed,#ec4899)"}}>{user.telegramPhotoUrl ? <img src={user.telegramPhotoUrl} alt="" className="h-full w-full object-cover"/> : user.firstName[0]}</div><div><p className="text-sm text-white">{user.firstName} {user.lastName}</p><p className="text-xs" style={{color:"var(--text-muted)"}}>{user.telegramUsername?`@${user.telegramUsername}`:"Username yo‘q"}</p></div></div></td><td className="p-4 text-xs" style={{color:"var(--text-muted)"}}>{user.telegramId}</td><td className="p-4"><span className="inline-flex items-center gap-1 rounded-md px-2 py-1 text-xs" style={{background:user.subscriptionType==="PREMIUM"?"rgba(217,70,239,.15)":"rgba(255,255,255,.06)",color:user.subscriptionType==="PREMIUM"?"#f0abfc":"#cbd5e1"}}>{user.subscriptionType==="PREMIUM"&&<Crown className="h-3 w-3"/>}{user.subscriptionType==="PREMIUM"?"Premium":"Oddiy"}</span></td><td className="p-4"><span className="inline-flex items-center gap-1 rounded-md px-2 py-1 text-xs" style={{background:user.role==="ADMIN"?"rgba(124,58,237,.18)":"rgba(255,255,255,.06)",color:user.role==="ADMIN"?"#c4b5fd":"#cbd5e1"}}>{user.role==="ADMIN"&&<Shield className="h-3 w-3"/>}{user.role}</span></td><td className="p-4 text-xs" style={{color:"var(--text-muted)"}}>{new Date(user.createdAt).toLocaleDateString("uz-UZ")}</td><td className="p-4 text-xs" style={{color:"var(--text-muted)"}}>{ago(user.lastLoginAt)}</td><td className="p-4"><span className="text-xs" style={{color:user.isActive?"#4ade80":"#fb7185"}}>{user.isActive?"Faol":"Bloklangan"}</span></td><td className="p-4"><button onClick={()=>setDrawer({...user})} aria-label="Tahrirlash" className="p-2 rounded-lg text-violet-300 hover:bg-violet-500/15"><Pencil className="h-4 w-4"/></button></td></tr>)}</tbody></table>{!data.users.length&&<div className="py-16 text-center text-sm" style={{color:"var(--text-muted)"}}>Foydalanuvchi topilmadi.</div>}</div><div className="p-4 flex items-center justify-between" style={{borderTop:"1px solid var(--border)"}}><span className="text-xs" style={{color:"var(--text-muted)"}}>{data.total} ta foydalanuvchi</span><div className="flex items-center gap-2"><button disabled={page===1} onClick={()=>setPage(page-1)} className="p-2 rounded-lg disabled:opacity-30"><ChevronLeft className="h-4 w-4"/></button><span className="text-sm text-white">{page} / {pages}</span><button disabled={page===pages} onClick={()=>setPage(page+1)} className="p-2 rounded-lg disabled:opacity-30"><ChevronRight className="h-4 w-4"/></button></div></div></div>{selected.length>0&&<div className="fixed bottom-6 left-1/2 -translate-x-1/2 z-50 rounded-2xl px-5 py-3 flex items-center gap-4 shadow-2xl" style={{background:"rgba(20,15,34,.94)",backdropFilter:"blur(18px)",border:"1px solid rgba(168,85,247,.55)"}}><span className="text-sm text-white">{selected.length} foydalanuvchi tanlandi</span><button onClick={()=>setDrawer({...selectedUsers[0]})} className="px-3 py-2 rounded-xl text-sm text-white" style={{background:"linear-gradient(135deg,#7c3aed,#db2777)"}}>Tahrirlash</button><button onClick={()=>setSelected([])} className="text-sm text-violet-300">Bekor qilish</button></div>}{drawer&&<aside className="fixed right-0 top-0 h-screen w-full max-w-md z-[60] p-6 overflow-y-auto" style={{background:"rgba(15,14,25,.98)",borderLeft:"1px solid rgba(168,85,247,.35)",boxShadow:"-20px 0 60px rgba(0,0,0,.38)"}}><button onClick={()=>setDrawer(null)} className="absolute right-5 top-5 text-gray-400"><X/></button><div className="flex items-center gap-3 mb-7"><div className="h-14 w-14 rounded-full flex items-center justify-center text-xl text-white" style={{background:"linear-gradient(135deg,#7c3aed,#ec4899)"}}>{drawer.firstName[0]}</div><div><h2 className="font-bold text-white">{drawer.firstName} {drawer.lastName}</h2><p className="text-sm" style={{color:"var(--text-muted)"}}>{drawer.telegramUsername?`@${drawer.telegramUsername}`:"Username yo‘q"}</p><p className="text-xs mt-1" style={{color:"var(--text-muted)"}}>Telegram ID: {drawer.telegramId}</p></div></div><label className="block text-sm mb-2" style={{color:"var(--text-muted)"}}>Role</label><select value={drawer.role} onChange={(e)=>setDrawer({...drawer,role:e.target.value})} className="w-full p-3 rounded-xl bg-black/20 text-white mb-5" style={{border:"1px solid var(--border)"}}><option value="USER">User</option><option value="ADMIN">Admin</option></select><label className="block text-sm mb-2" style={{color:"var(--text-muted)"}}>Premium maqomi</label><select value={drawer.subscriptionType} onChange={(e)=>setDrawer({...drawer,subscriptionType:e.target.value as StoredUser["subscriptionType"]})} className="w-full p-3 rounded-xl bg-black/20 text-white mb-5" style={{border:"1px solid var(--border)"}}><option value="FREE">Oddiy</option><option value="PREMIUM">Premium</option></select><label className="block text-sm mb-2" style={{color:"var(--text-muted)"}}>Hisob holati</label><select value={drawer.isActive?"ACTIVE":"BLOCKED"} onChange={(e)=>setDrawer({...drawer,isActive:e.target.value==="ACTIVE"})} className="w-full p-3 rounded-xl bg-black/20 text-white mb-7" style={{border:"1px solid var(--border)"}}><option value="ACTIVE">Faol</option><option value="BLOCKED">Bloklangan</option></select><div className="flex gap-3"><button onClick={()=>setDrawer(null)} className="flex-1 py-3 rounded-xl text-white" style={{border:"1px solid var(--border)"}}>Bekor qilish</button><button disabled={saving} onClick={save} className="flex-1 py-3 rounded-xl text-white" style={{background:"linear-gradient(135deg,#7c3aed,#db2777)"}}>{saving?"Saqlanmoqda...":"Saqlash"}</button></div></aside>}</div>;
}
